# Stories

Status: future Storybook migration target.

Stories should render real shared primitives/components with fixture data. They
should not reproduce the UI with loose HTML, inline styles, or screenshot-like
mockups.

The extension workbench remains the interim state harness until these stories
exist.
